### Was muss man machen?

1. Docker build 
2. Docker push zur Docker Registry
3. Docker push zur github registry
(brauch man damit kubectl auf das git repo zugriff hat)

## Build
1. docker build mit -t
2. docker tag mit imange für ghrc.io und dockerhub
3. docker push 
4. values anpassen
